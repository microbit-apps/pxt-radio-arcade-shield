// init.ts needs to redirect to here.

/**
* Decode incoming data,
* Map it to appropriate screen.ts function + params
* Invoke it.
*/
export function radioScreenRx(): void {
    decodeCommand();
}

function decodeCommand(): void {

}
